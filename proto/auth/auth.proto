syntax = "proto3";

package auth;

option go_package = "github.com/GoSMRiST/protosLibary/gen/go/auth;auth";

service Auth {
  rpc ValidateToken (ValidateTokenRequest) returns (ValidateTokenResponse);
}

message ValidateTokenRequest {
  string token = 1;
}

message ValidateTokenResponse {
  int64 user_id = 1;
  string role = 2;
}